pid             nginx.pid;
daemon          off;

events {
    worker_connections  768;
}

http{
    error_log       error.log;
    access_log      access.log;

    server {
        listen          0.0.0.0:8080;
        root            .;
        location  /  {
            autoindex       on;
        }
    }
}
